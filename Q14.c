/*
Q . 14 : Accept String From User , count number of vowels present in that string as
well as ocurences of every vowel .
*/

#include"function.h"

int main() {

	char str[SIZE];

	input(str);

	countVowels(str);

	return 0;
}